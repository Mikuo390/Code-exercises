#include<stdio.h>
int main()
{
	int a, b;
	int tmp = 0;
	printf("a和b的值：");
	scanf("%d%d", &a, &b);
		
	printf("交换前a=%d,b=%d\n", a, b);
	tmp = a;
	a = b;
	b = tmp;
	printf("交换后a=%d,b=%d\n", a, b);
	return 0;
}
