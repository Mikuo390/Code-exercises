#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<ctype.h>
int main()
{
	//char ch = 'w';
	//int ret = islower(ch);
	//int ret = isdight(ch);
	//printf("%d\n", ret);

	//char ch = tolower('Q');
	//char ch = toupper('q');
	//putchar(ch);

	char arr[] = "I am a student";
	int i = 0;
	while (arr[i])
	{
		if (islower(arr[i]))//if (isupper(arr[i]))
		{
			arr[i] = tolower(arr[i]);
		}
		i++;
	}
	printf("%s\n",arr);
	return 0;
}
