#include<stdio.h>
int main()
{
	int num = 0;
	int count = 0;
	scanf("%d", &num);//3-011
	
	//统计num的补码有几个1
	while (num)
	{
		if (num % 2 == 1)
		{
			count++;
			
		}
		num = num / 2;
	}
	printf("%d\n", count);
	return 0;
}
