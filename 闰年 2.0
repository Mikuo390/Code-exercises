#include<stdio.h>
int main()
{
	int year = 0;
	int count = 0;
	for (year = 0; year <= 2000; year++)
	{
		if (((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0))
		{
			printf("%d\n", year);
			count++;
		}
	}
	printf("\n闰年数量=%d\n", count);
	return 0;
}
