#include<stdio.h>
int main()
{
	int a, b, c, max;
	printf("请输入三个数的值:>");
	scanf("%d%d%d", &a, &b, &c);
	if (a > b && a > c) 
	{
		max = a;
		printf("最大值:>%d\n", a);
	}
	else if (c > a && c > b) 
	{
		max = c;
		printf("最大值:>%d\n", c);
	}
	else
	{ 
		max = b;
		printf("最大值:>%d\n", b);	
	}
}
