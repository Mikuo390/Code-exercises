#include<stdio.h>
get_max(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}
int main()
{
	int a, b;
	printf("a和b的值：");
	scanf("%d%d", &a, &b);
	
	int max=get_max(a,b);
	printf("max=%d\n", max);
	return 0;
}
