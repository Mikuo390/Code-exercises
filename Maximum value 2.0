#include<stdio.h>
Max(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}
int main()
{
	int a, b;
	int max = 0;
	printf("请输入两个数字:\n");
	scanf("%d%d",&a,&b);

	max = Max(a, b);
	printf("max=%d\n", max);

	return 0;
}
